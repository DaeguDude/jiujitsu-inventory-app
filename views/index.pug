extends layout

block content
  h1= title
  p Welcome to #[em Jiujitsu Gi Inventory App].

  if error
    p Error getting dynamic content.
  else
    p The jiujitsu gi inventory app has the following record counts:

    ul
      li #[strong Brands:] !{data.brand_count}
      li #[strong Gis:] !{data.gi_count}
      